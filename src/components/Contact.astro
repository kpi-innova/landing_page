---

---

<section id="contacto" class="py-24 bg-gray-50 dark:bg-black/50 relative">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="lg:grid lg:grid-cols-2 lg:gap-16 items-start">
      <!-- Contact Info Side -->
      <div class="mb-12 lg:mb-0">
        <h2
          class="text-3xl font-bold font-heading text-dark dark:text-light sm:text-4xl mb-6"
        >
          <span data-i18n="contact.title">Hablemos de tu</span>
          <br />
          <span data-i18n="contact.title.break">Próximo</span>
          <span class="text-primary" data-i18n="contact.title.highlight"
            >Proyecto</span
          >
        </h2>
        <p
          class="text-lg text-gray-600 dark:text-gray-400 mb-8"
          data-i18n="contact.text"
        >
          ¿Listo para optimizar tu planta? Nuestros ingenieros están listos para
          analizar tus requerimientos y proponerte una solución a medida.
        </p>
      </div>

      <!-- Form Side -->
      <div
        class="bg-white dark:bg-dark-surface p-8 rounded-sm shadow-2xl border border-gray-200 dark:border-gray-800"
      >
        <form
          class="space-y-6"
          action="/api/contact"
          name="contact"
          method="POST"
          data-netlify-recaptcha="true"
          netlify-honeypot="bot-field"
          data-netlify="true"
        >
          <input type="hidden" name="form-name" value="contact" />
          <p class="hidden">
            <label
              >Don’t fill this out if you’re human: <input
                name="bot-field"
              /></label
            >
          </p>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
              <label
                for="name"
                class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1"
                data-i18n="contact.form.name">Nombre</label
              >
              <input
                type="text"
                name="name"
                id="name"
                required
                class="block w-full rounded-sm border-gray-300 dark:border-gray-700 bg-gray-50 dark:bg-black/20 text-dark dark:text-white focus:ring-primary focus:border-primary transition-colors"
              />
            </div>
            <div>
              <label
                for="phone"
                class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1"
                data-i18n="contact.form.phone">Teléfono</label
              >
              <input
                type="tel"
                name="phone"
                id="phone"
                required
                class="block w-full rounded-sm border-gray-300 dark:border-gray-700 bg-gray-50 dark:bg-black/20 text-dark dark:text-white focus:ring-primary focus:border-primary transition-colors"
              />
            </div>
          </div>

          <div>
            <label
              for="company"
              class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1"
              data-i18n="contact.form.company">Empresa</label
            >
            <input
              type="text"
              name="company"
              id="company"
              required
              class="block w-full rounded-sm border-gray-300 dark:border-gray-700 bg-gray-50 dark:bg-black/20 text-dark dark:text-white focus:ring-primary focus:border-primary transition-colors"
            />
          </div>

          <div>
            <label
              for="email"
              class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1"
              data-i18n="contact.form.email">Email Corporativo</label
            >
            <input
              type="email"
              name="email"
              id="email"
              required
              class="block w-full rounded-sm border-gray-300 dark:border-gray-700 bg-gray-50 dark:bg-black/20 text-dark dark:text-white focus:ring-primary focus:border-primary transition-colors"
            />
          </div>

          <div>
            <label
              for="message"
              class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1"
              data-i18n="contact.form.message">¿Cómo podemos ayudarte?</label
            >
            <textarea
              id="message"
              name="message"
              rows="4"
              required
              class="block w-full rounded-sm border-gray-300 dark:border-gray-700 bg-gray-50 dark:bg-black/20 text-dark dark:text-white focus:ring-primary focus:border-primary transition-colors"
            ></textarea>
          </div>

          <div data-netlify-recaptcha="true"></div>

          <button
            type="submit"
            class="w-full flex justify-center py-4 px-4 border border-transparent rounded-sm shadow-sm text-base font-bold text-dark bg-primary hover:bg-primary/90 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-all uppercase tracking-wide"
            data-i18n="contact.form.submit"
          >
            Solicitar Demo
          </button>
        </form>
      </div>
    </div>
  </div>
</section>
