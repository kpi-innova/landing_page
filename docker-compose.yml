services:
  web:
    build: .
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.astro
    environment:
      - HOST=0.0.0.0
    command: npm run dev -- --host 0.0.0.0
